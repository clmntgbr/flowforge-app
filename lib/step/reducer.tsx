import { StepAction, StepState } from "./types"

export const stepReducer = (
  state: StepState,
  action: StepAction
): StepState => {
  switch (action.type) {
    default:
      return state
  }
}
