import { ConnexionAction, ConnexionState } from "./types"

export const ConnexionReducer = (
  state: ConnexionState,
  action: ConnexionAction
): ConnexionState => {
  switch (action.type) {
    default:
      return state
  }
}
